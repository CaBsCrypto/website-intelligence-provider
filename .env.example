# Public Testnet configuration. Copy to .env.local; never commit real secrets.
X402_PUBLIC_BASE_URL=http://127.0.0.1:8787
X402_PAY_TO=
X402_FACILITATOR_URL=
X402_SETTLEMENT_ENABLED=false

# Optional facilitator credential. Leave empty unless your selected facilitator requires it.
X402_FACILITATOR_BEARER_TOKEN=
